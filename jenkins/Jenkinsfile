pipeline {
    agent any
    environment {
        CC = 'clang'
    }

    stages {
        stage('Build') {
            environment {
                DEBUG_FLAGS = '-g'
            }
            steps {
                echo 'git status'
                echo 'Start Ansible playbook..'
                echo 'Complete.'
                echo '------------------------'
                sh 'printenv'
                
            
            }

        }
        

    }
}
