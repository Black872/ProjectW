---
- name: testbook
  hosts: DEV
  become: true

  tasks:
    # yamllint disable-line rule:comments-indentation
    # - name: Check is workfolder exist
    #   shell: ls
    #   register: workfolder_out

    # - name: debug workfodler
    #   debug:
    #     var: workfolder_out.stdout_lines[0]
    #   #create folder for docker compose files
    # - name: Create workfolder on remote
    #   file:
    #     name: ecomerce
    #     path: /home/dark/ecomerce
    #     state: directory
    #     mode: "0755"
    #   when: workfolder_out
    # worked
    # - name: Ansible | Print 'lsb_release'
    #   debug:
    #     msg: "{{ ansible_distribution_release }}"
    #   register: lsb_release
    # - name: debug success
    #   debug:
    #     var: lsb_release
    # - name: successs
    #   debug:
    #     msg: success
    #   when: lsb_release.msg == "jammy"
    # worked 2
    # - block:
    #     - name: Check is workfolder exist
    #       shell: ls
    #       register: workfolder_out
    #     - name: debug workfodler
    #       debug:
    #         var: workfolder_out.stdout_lines
    #     # Сreate folder for docker compose files
    #     - name: Create workfolder on remote
    #       file:
    #         name: ecomerce
    #         path: /home/dark
    #         state: directory
    #         mode: "0755"
    #       when: workfolder_out.stdout_lines[3] != "ecomerce"
    #   when: ansible_os_family == "Debian"
